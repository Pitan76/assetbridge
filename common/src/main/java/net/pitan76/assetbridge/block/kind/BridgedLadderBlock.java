package net.pitan76.assetbridge.block.kind;

import net.minecraft.world.level.block.LadderBlock;

/** A bridged block whose model says it is a ladder. */
public class BridgedLadderBlock extends LadderBlock {
    public BridgedLadderBlock(Properties properties) {
        super(properties);
    }
}
