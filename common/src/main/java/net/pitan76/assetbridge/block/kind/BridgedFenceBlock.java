package net.pitan76.assetbridge.block.kind;

import net.minecraft.world.level.block.FenceBlock;

/** A bridged block whose model says it is a fence. */
public class BridgedFenceBlock extends FenceBlock {
    public BridgedFenceBlock(Properties properties) {
        super(properties);
    }
}
